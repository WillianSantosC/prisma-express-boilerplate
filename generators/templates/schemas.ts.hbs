import { z } from 'zod';

export const {{camelCase name}}Schema = {
    create: z.object({
        
    }),

    update: z.object({
        
    }),

    retrieve: z.object({
        
    }),

    delete: z.object({
        
    })

};
